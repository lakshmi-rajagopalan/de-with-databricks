resources:
  genie_space:
    clickstream_workshop_space:
      name: clickstream-workshop-space
      catalog: ${var.catalog_name}
      target: ${resources.schemas.workshop.name}
      channel: CURRENT
      serverless: true
      configuration:
        space_url: ${var.genie_space_url}